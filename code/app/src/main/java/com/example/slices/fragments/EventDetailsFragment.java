package com.example.slices.fragments;

import android.view.Menu;
import android.view.MenuItem;

import androidx.fragment.app.Fragment;

/** EventDetailsFragment
 * A fragment for displaying the details of a tapped-on event
 * - R.P.
 */
public class EventDetailsFragment extends Fragment {
    // TODO: add fragment information to instantiate the details of an event from DB (may need Ryan)\

/* todo: back button code for later
    getSupportActionBar().setDisplayHomeAsUpEnabled(true);
}
 ---> Stuff from S. Overflow:
@Override
public boolean onOptionsItemSelected(MenuItem item) {
    switch (item.getItemId()) {
        case android.R.id.home:
            finish();
            return true;
    }
    return super.onOptionsItemSelected(item);
}

public boolean onCreateOptionsMenu(Menu menu) {
    return true;
 */
}
